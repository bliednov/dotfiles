#!/usr/bin/env bash

set -eufo pipefail

{{ template "env.tmpl" . }}

{{- if .gcp }}
sudo passwd -d $USER
{{- end }}

zsh_path=$(which zsh)
if [[ "${zsh_path}" != "${SHELL}" ]] && [[ "${zsh_path}x" != "x" ]]; then
    sudo chsh -s $zsh_path root
    sudo chsh -s $zsh_path $USER
fi
